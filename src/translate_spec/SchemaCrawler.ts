import Parameter from "./components/Parameter";
import BaseSchema from "./components/schemas/BaseSchema";

export default class SchemaCrawler {
    static schemas: Record<string, BaseSchema> = {};

    static crawl(p: Parameter) {

    }
}